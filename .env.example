# Binance Connector Configuration
# Copy this to .env for custom settings (optional - not needed for market data!)

# Use testnet (true) or mainnet (false)
# Mainnet = real prices, Testnet = fake testing data
BINANCE_TESTNET=false

# Request timeout in seconds (default: 10)
BINANCE_TIMEOUT_SECONDS=10

# Maximum requests per minute (default: 1200)
# Binance allows 1200 req/min, lower this if being conservative
BINANCE_REQUESTS_PER_MINUTE=1200

# ============================================
# OPTIONAL: Only needed for trading features
# (not yet implemented)
# ============================================

# BINANCE_API_KEY=your_api_key_here
# BINANCE_SECRET_KEY=your_secret_key_here
